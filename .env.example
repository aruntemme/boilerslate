# Docker Compose settings for local development.
# Host port for Postgres. Not 5432, so the stack never collides with a
# Postgres already installed on your machine.
POSTGRES_PORT=5433
POSTGRES_PASSWORD=password
